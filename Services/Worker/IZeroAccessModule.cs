namespace Artimus.Services.Worker {
	/**
	 * Every Stargate can have a zero access module to encapsulate
	 * thread-critical data and provide core service functionality.
	 */
	public interface IZeroAccessModule { }
}